[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Show verbose output
addopts = -v --tb=short

# Coverage options (optional, run with: pytest --cov)
# addopts = -v --tb=short --cov=app --cov-report=term-missing --cov-report=html
