[project]
name = "data-ingest"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "starlette>=0.41",
    "uvicorn>=0.32",
]

[dependency-groups]
dev = [
    "httpx>=0.27",
    "pytest>=8.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
