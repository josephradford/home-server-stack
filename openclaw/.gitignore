# OpenClaw build artifacts and temporary files

# Ignore patches directory (created during build)
patches/

# Ignore any downloaded OpenClaw source
openclaw/

# Keep only Dockerfile and documentation
!Dockerfile
!README.md
!.gitignore
